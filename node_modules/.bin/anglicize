#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../anglicize/bin/anglicize" "$@"
  ret=$?
else 
  node  "$basedir/../anglicize/bin/anglicize" "$@"
  ret=$?
fi
exit $ret
